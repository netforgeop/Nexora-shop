{
  "name": "nexora-shop",
  "version": "1.0.0",
  "description": "Nexora — premium multi-category e-commerce HTML template (RTL/LTR ready)",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node scripts/build.mjs",
    "watch": "node scripts/build.mjs --watch",
    "serve": "python3 -m http.server 8080 --bind 0.0.0.0",
    "dev": "npm run build && npm run serve"
  },
  "devDependencies": {
    "@fontsource/inter": "^5.1.0",
    "bootstrap": "^5.3.3",
    "esbuild": "^0.24.2",
    "photoswipe": "^5.4.4",
    "swiper": "^11.1.0"
  }
}
